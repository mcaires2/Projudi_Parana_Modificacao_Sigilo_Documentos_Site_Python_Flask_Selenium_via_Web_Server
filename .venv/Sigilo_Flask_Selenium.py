from app import app

# python -m venv venv - the second name is the dir name that you wish
# set FLASK_APP=microblog.py  -> to set the Sigilo_Flask_Selenium.py as application default (windows env) (export instead of set to linux env) -> every time you run you do need to set this
# to set it continuosly have to install pip install python-dotenv on power shell and create a file called .flaskenv
# on this created file, type FLASK_APP=Sigilo_Flask_Selenium.py and save it to the same directory where you saved our "app.py" here = "Sigilo_Flask_Selenium.py"
# flask run to run the site
#control + C to quit the running site